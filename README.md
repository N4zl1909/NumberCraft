A simple Python program to calculate the GCD (Greatest Common Divisor) and LCM (Least Common Multiple) of two numbers.

Features
Quick GCD and LCM calculation

Interactive loop (runs until you quit)

Type q anytime to exit
